The canonical sanitized provider fixtures live in `contracts/usage/fixtures` at
the repository root. Provider tests resolve that directory relative to their
source file so the same bytes can also be consumed by non-Swift contract tests.
